package com.example.Tiendaenlinea.repositories;

import com.example.Tiendaenlinea.entities.ImageData;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.data.jpa.repository.Query;
import org.springframework.data.repository.query.Param;

/*public interface ImageDataRepository extends JpaRepository<ImageData, Integer> {

    @Query("SELECT id FROM ImageData id WHERE id.imageId = :imageId")
    ImageData findImageDataById(@Param("imageId") Integer imageId);
}*/
