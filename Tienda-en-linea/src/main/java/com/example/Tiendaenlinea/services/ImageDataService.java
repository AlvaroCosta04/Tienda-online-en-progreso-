package com.example.Tiendaenlinea.services;

/*
import com.example.Tiendaenlinea.entities.ImageData;
import com.example.Tiendaenlinea.repositories.ImageDataRepository;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Service;
import org.springframework.transaction.annotation.Transactional;


@Service*/
public class ImageDataService {

    /*private final ImageDataRepository imageDataRepository;

    @Autowired
    public ImageDataService(ImageDataRepository imageDataRepository) {
        this.imageDataRepository = imageDataRepository;
    }

    @Transactional(readOnly = true)
    public ImageData findImageDataById(Integer imageId) {
        return imageDataRepository.findImageDataById(imageId);
    }

    @Transactional
    public ImageData createImageData(ImageData imageData) {
        return imageDataRepository.save(imageData);
    }

    @Transactional
    public ImageData updateImageData(ImageData imageData) {
        return imageDataRepository.save(imageData);
    }

    @Transactional
    public void deleteImageData(Integer imageId) {
        imageDataRepository.deleteById(imageId);
    }*/
}
